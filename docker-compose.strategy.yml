version: "3.9"

services:
  strategy:
    build:
      context: .
      dockerfile: strategy/Dockerfile
    environment:
      STRATEGY_DB_PATH: /app/data/strategies.db
    ports:
      - "8100:8100"
    volumes:
      - ./strategy:/app/strategy
      - ./docs:/app/docs
      - ./data:/app/data
