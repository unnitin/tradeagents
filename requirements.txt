# Core data analysis
pandas>=1.3.0
numpy>=1.21.0
PyYAML>=6.0

# HTTP requests for data fetching
requests>=2.25.0
urllib3>=1.26.0

# AI/ML libraries for NLP and ML
transformers>=4.30.0    # for FinBERT and any future NLP tasks
torch>=2.0.0            # PyTorch backend for transformers (CPU version is fine to start)
# (Optional: if using GPU, install the appropriate torch version with CUDA support manually)

# Technical analysis (if using any external libs, but our indicators are custom; no ta-lib needed)
# ta-lib (Optional) – not used currently, but if needed for extended indicators.

# Utility libraries
python-dotenv>=1.0.0    # for loading .env files (if using, optional)

# Testing and dev (optional to include in requirements or as dev requirements)
pytest>=7.0.0           # if using pytest for tests
# (unittest is in stdlib, so no package needed for that)
